package tratamentoErros;

public class Conceito {

	/*
	 * Erros de compilação: o compilador não irá executar o processo
	 * Erros de tempo de execucao: acontece quando já foi compilado e o usuario está utilizando
		throwable: aplicação para e lança este erro
			Error: mais critico
			Exception: tratar (fluxo normal)
				checada(Exception): ou trata ou passa para lança para outro metodo
				não checada(runTimeException): nao precisa colocar na assinatura do metodo e pode passar sem ser percebida entre os metodos
	 * 
	 */
	
	/* Pilha de metodos: vai colocando um metodo após o outro, quando chega no final vai retirando da pilha até o metodo main
	 *	 quando da excecao, quando o erro for mostrado mostra a pilha de execucao
	 */
	
	
}
