package StreamAPI;

public class ConceitoTiposOperacao {
	
	/*
	 * build ops: criar uma stream
	 * interm ops: transformar os dados
	 * terminal ops: pode gerar algo diferente de stream
	 * 		build -> intem -> interm -> ... -> terminal
	 *  
	 *  Streams ordenadas: tem um sequencia a ser seguidas
	 *  Streams não ordenadas: não precisam necessariamente de uma ordem
	 *  	Stream sequencial: um dado por vez
	 *  	Stream parelela: internamente cria varias linhas de execução(processamento)
	 */

}
